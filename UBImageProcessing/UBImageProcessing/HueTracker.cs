using System;
using System.Windows.Forms;

namespace UBImageProcessing
{
    public partial class HueTracker : Form
    {
        public HueTracker()
        {
            InitializeComponent();
        }

       
        private void HueTracker_Load(object sender, EventArgs e)
        {

        }

        private void btnDone_Click(object sender, EventArgs e)
        {
            //float hue = (float)(setHue.Value / 256.0);
            //Bitmap dest = ColorSpace.Hue(src, luminance);
            //Luminance.Text = luminance.ToString();
            //Invalidate(true);

        }
    }
}
